import { createContext, useContext } from "react";

const Contextvalue = createContext(null);
export const Cartvalues = createContext();
export let CartPriceValue = createContext();



export default Contextvalue;
